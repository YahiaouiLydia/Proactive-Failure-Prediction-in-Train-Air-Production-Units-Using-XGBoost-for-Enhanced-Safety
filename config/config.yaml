# config/config.yaml

data:
  raw: "../data/raw/MetroPT3(CompressorDatase).csv"
  processed: "../data/processed/"

paths:
  models: "../models/"

hyperparameters:
  xgboost:
    n_estimators: [50, 100, 150]
    max_depth: [3, 5, 7]
    learning_rate: [0.01, 0.1, 0.3]